func fibonacci(n: Int32) -> Int32 {
    var a = 0;
    var b = 1;
    var i = 0;
    while i < n {
        let next = a + b;
        a = b;
        b = next;
        i = i + 1;
    };
    return a;
}

func sum_evens(n: Int32) -> Int32 {
    var i = 0;
    var s = 0;
    while i < n {
        i = i + 1;
        if (i / 2) * 2 != i {
            continue;
        };
        s = s + i;
    };
    return s;
}

func main() -> Int32 {
    return fibonacci(10) + sum_evens(10);
}
